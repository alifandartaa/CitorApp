package com.example.citorapp.home

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.ArrayAdapter
import com.example.citorapp.R
import com.example.citorapp.databinding.ActivityPelayananBinding

class  PelayananActivity : AppCompatActivity() {
//    private var bindingPelayanan: ActivityPelayananBinding? = null
//    private val binding get() = bindingPelayanan!!

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_pelayanan)
//        bindingPelayanan = ActivityPelayananBinding.inflate(layoutInflater)
//
//        val kategori = resources.getStringArray(R.array.kategori)
//        val arrayAdapter = ArrayAdapter(this@PelayananActivity, R.layout.dropdown_item, kategori)
//        binding.autoCompleteTextView.setAdapter(arrayAdapter)

    }
}