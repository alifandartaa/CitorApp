<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.citorapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CitorApp">
<!--        <activity android:name=".home.detailVendor.BookingActivity" />-->

        <activity android:name=".home.pembayaran.PembayaranActivity3">

        </activity>

        <activity android:name=".home.pembayaran.PembayaranActivity2">

        </activity>
        <activity android:name=".home.pembayaran.PembayaranActivity1">

        </activity>

        <activity android:name=".home.PelayananActivity">

        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".auth.login.EmailConfirmActivity" />
        <activity android:name=".ProfileTestActivity" />
        <activity
            android:name=".auth.AuthActivity"
            android:windowSoftInputMode="stateHidden|adjustPan">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".home.detailVendor.DetailVendorActivity" />
        <activity android:name=".auth.login.OtpInputActivity" />
    </application>

</manifest>